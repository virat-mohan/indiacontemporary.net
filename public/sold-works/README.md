Drop genuine sold-artwork photos here (e.g. `terracotta-memory-sold.jpg`).

Once a file is here, reference it in `src/data/artists.js` on the matching
`pastSales` entry as `image: "/sold-works/terracotta-memory-sold.jpg"` — the
`public/` folder is served from the site root, so no import is needed.
